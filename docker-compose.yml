version: '3'
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mi_bd
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
